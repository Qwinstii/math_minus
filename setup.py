from setuptools import setup, find_packages

VERSION = "1.0"
DESCRIPTION = "math-"
LONG_DESCRIPTION = "math- its a math package but minus :) "

setup(
    name="math-",
    version=VERSION,
    author="Qwinstii",
    author_email="<qwinsticoder@gmail.com>",
    description=DESCRIPTION,
    packages=find_packages(),
    install_requires=[],
    keywords=['python', 'add', 'sub', 'mul', 'div', 'pwr'],
    classifiers=[
        "",
    ]
)